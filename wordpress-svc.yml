apiVersion: v1
kind: Service
metadata:
  name: wordpress-svc
spec:
  type: ClusterIP
  selector:
    matchLabels:
      tier: frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
